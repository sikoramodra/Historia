<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Historyproj" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/W</item>
        <item name="colorPrimaryVariant">@color/R</item>
        <item name="colorOnPrimary">@color/R</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/W</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:background">@color/F</item>
        <!-- Customize your theme here. -->
    </style>
</resources>